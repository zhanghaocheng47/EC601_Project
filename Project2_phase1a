#imort tweepy after install it
import tweepy
 
#connect this with my own twitterAPI
consumer_key = 
consumer_secret =
access_token = 
access_token_secret =
 
auth = tweepy.OAuthHandler(consumer_key, consumer_secret)
auth.set_access_token(access_token, access_token_secret)
 
api = tweepy.API(auth)

#show the timeline on my home page

public_tweets = api.home_timeline()
for tweet in public_tweets:
    print tweet.text
    

#for other functions, example: retrieve all the fans of one twitter id

api=tweepy.API(auth,wait_on_rate_limit=True)results=api.friends(id="XXXXX")
